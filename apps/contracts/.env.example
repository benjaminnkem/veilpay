# Optional: set via hardhat keystore instead of env vars
# hardhat keystore set SEPOLIA_RPC_URL
# hardhat keystore set SEPOLIA_PRIVATE_KEY

SEPOLIA_RPC_URL=
SEPOLIA_PRIVATE_KEY=
ETHERSCAN_API_KEY=

# Enable only for official local Nox E2E tests; requires Docker daemon.
NOX_E2E=0
